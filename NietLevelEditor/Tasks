Add direction to checkpoint
	add modifiable direction AND NUMBER

Display font for colored case (Player departure, ground trigger, checkpoint, secret)


Add level size modifiable

after reopen grid update size grid
